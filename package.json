{
  "name": "sceauid",
  "private": true,
  "version": "0.0.0",
  "description": "Passkey-first identity infrastructure for modern applications.",
  "packageManager": "pnpm@10.2.1",
  "scripts": {
    "build": "turbo build",
    "check": "biome check .",
    "format": "biome format --write .",
    "dev": "turbo dev",
    "lint": "turbo lint && biome check .",
    "test": "turbo test",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  }
}
